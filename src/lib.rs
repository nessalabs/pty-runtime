//! Bounded PTY session ownership and replaceable terminal projection.
//!
//! Implementation is in progress. See the ADR verification ledger for evidence.
#![forbid(unsafe_code)]

pub use pty_runtime_domain as domain;
