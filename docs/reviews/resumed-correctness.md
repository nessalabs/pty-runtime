# Resumed independent adversarial correctness review

Reviewed the working tree based on `46dbec64384e4519e751086ef47feb5bac11024d` on 2026-09-08. Read AGENTS.md and coding_standards.md. Scope: all changed production code, helper materialization hooks/fixtures, projection I/O fixtures checked against production I/O orchestration, and final load census behavior. This reviewer ran no Rust build or gate; the parent coordinates those. Historical artifacts are evidence only for their recorded source identity.

## P2: missing final cleanup census can receive a passing result

`scripts/release/load.py:96` validates cleanup only when a `closed` checkpoint arrives. Its success path at lines 109–137 requires exit 0 and complete but never requires `closed`. A producer emitting measurement_start, measurement_end, ready, complete and exiting normally before periodic sampling receives `trial_result passed=True`, without the final descendant/zombie census. This predates the one-line sampling fix, but blocks acceptance of the final-census contract.

Independently added `test_clean_exit_without_closed_census_is_rejected` to `scripts/tests/test_load_final_census.py`. The real child exits normally; `advance_clock=False` avoids periodic sampling and the forced poll race. Existing negative control using ready advances the clock and fails because the collector throws, so it did not prove this case. Retained `work/resumed-validation/missing-closed-red.log`: 5 tests, exactly the new test fails (True is not false), command `python3 -m unittest discover -s scripts/tests -p test_load_final_census.py -v`, exit 1.

**Resolved and independently re-reviewed:** the parent added `assert 'closed' in checkpoints` immediately after successful exit/complete validation and before trial results. A census exception or invalid final snapshot still exits through the failure path before success. Re-ran the same five tests, all pass, exit 0, retained `work/resumed-validation/missing-closed-green.log`. No P1/P2 remains in this reviewed scope.

The subsequent complete gate exposed two older success fixtures in `scripts/tests/test_load_diagnostics.py` that omitted `closed`; original failures remain in `docs/verification/resumed/macos-gate/command.log`. This reviewer updated both child protocols to emit and await acknowledgement of `closed`, and their census doubles to return the actual phase, one process and zero zombies. The unavailable-idle-CPU test still requires an unknown target result without a correctness failure; the owned-pipes test still requires final stderr, successful exit, closed stdin/stdout and trial_result as the final event. No production validation was weakened. Complete Python suite command `python3 -m unittest discover -s scripts/tests -v`: 43 tests pass, exit 0, retained `work/resumed-validation/python-all-green.log`. These historical fixture failures are not additional production defects.

## Helper image ownership and cleanup

No additional P1/P2 found in the changed Rust production code. The materializer exclusively opens the writable executable in its child, while the parent only owns immutable embedded bytes and the path. Success requires close and terminal wait status before publication. Parent forks therefore do not inherit that writer. Raw child operations use immutable inherited buffers, no allocation or destructors, checked progress, EINTR retry and `_exit`; failed writes leave cleanup to process exit and the parent's HelperImage Drop. Signal mask restoration occurs before wait, including fork failure, and restoration failure after successful fork does not bypass reaping. Typed error reduction is consistent with the public error categories.

`image_materialize.rs:99–106` retains child ownership after nonterminal traced statuses. Its blocking wait deliberately has no wall-clock bound, as documented in backend.rs. Host atfork handlers and exclusive SIGCHLD ownership remain explicit integration constraints, not newly established guarantees. No domain/registry lock is held across materialization.

The actual-open hook synchronizes after the writer opens and before it closes. The observer runs in the parent, and its parent-PID branch preserves observability for a parent-writer mutation. Callback unwind releases the child before reaping and resuming panic. The unrelated pre-exec child uses a bounded raw handshake; RAII releases and joins it even on assertions. This is a meaningful regression test, but the strongest proof still needs the same fixture and unchanged hook to fail on a parent-writer mutation on Linux and pass with the materializer on both target platforms.

Independently reviewed the fixture correction from `/bin/true` to `/usr/bin/true` at `process_image_fork.rs:52`. The latter exists on the reviewed macOS host. The retained `work/resumed-validation/gate.log` reports failure in blocker release/reap with NotFound, not an executable-image ETXTBSY failure. This is a fixture executable-path correction; it leaves the post-fork handshake, actual-open observation and both helper-execution assertions unchanged. Do not describe the original fixture failure as production-defect RED evidence. Parent owns the rerun and Linux path verification.

## Projection contracts

No P1/P2 found in the added tests. Wrong terminal descriptor and excessive Vec capacity exercise distinct pre-protection checks and require no provider calls, retained resident model, released leases, no persisted source, then valid retry preserving bytes. Provider faults distinguish authentication failure from malformed key/descriptor/empty/oversized ciphertext; retry at 64 versus 65 validates the backoff.

The pressure tests exercise partial admission rollback (1,024 plaintext fits, 1,064 protected bytes does not), resident quota rollback, pending view with ordered staged bytes, and rejected garbage-delete submission preserving the same source and storage charge while resident output progresses. Recovery asserts exact content, one restore/feed, one deletion, and zero final quotas. These are application orchestration proofs; fake terminal/protector/store implementations cannot establish native round-trip or encryption behavior.

## Remaining proof requirements

- P2 final-census requirement resolved with retained RED/GREEN as above; preserve both with acceptance evidence.
- Run final-source helper tests and unchanged-hook Linux mutation. Existing helper source manifests describe earlier hashes and do not alone prove current compiled source execution.
- Materializer direct tests cover success bytes/mode/mask, missing parent and existing destination. Child signal death, interrupted wait/write, permission failure, partial write failure, chmod/close failure, fork failure and restoration failure remain unproved error branches. Where reliably injectable, preserve typed error, reaping, mask, and private-directory cleanup assertions; do not claim exhaustive coverage from the three direct tests.
- A callback-panic test should verify the observer unwind path releases/reaps the child and removes the staging directory. Existing source supports it, but no explicit assertion currently proves that cleanup path.
- Run the repository gate after changes, scoped performance with exact source/platform identity, and coverage inventory. Prior Linux application results and historical load Python tests are useful scoped records, not final-tree gate or full release acceptance. Full repeats, lifecycle/reconnect workloads, 12-hour soak, native/platform coverage and 100% measured readiness remain separate requirements.
