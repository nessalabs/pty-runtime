# Public boundary contract inventory at 71c1d6f

This is a bounded read-only inventory, not an all-export proof or a new code review verdict. The facade groups into twelve finite boundary categories below. Existing validation and executed assertions are distinguished from specific missing assertions. No native crash/corpus work, tests, builds or source edits were performed.

## Evidence basis

Public surface: `src/lib.rs` re-exports runtime/session/replay, portable process/projection/checkpoint/terminal values, injected ports, optional event-stream adapter, and fixed diagnostics. `event_stream::transport` intentionally exposes the pinned external package under an infrastructure namespace; its cursor is distinct from the PTY byte cursor. It is not a promise that all third-party exports inherit the wrapper's redaction or portable-value guarantees.

All 22 test names cited in the matrix below were found as successful executions in `docs/verification/reader-memory-gauges/macos-gate-reviewed/command.log`, whose metadata records pass and unchanged source. Exact matched lines are in `docs/verification/public-boundary-contracts-71c1d6f/executed-tests.json`. The report does not infer execution from a test declaration or Rustdoc. Exact inspected file and gate hashes are in `source.json`. The gate's native tests are existing evidence only; none were rerun for this inventory.

## Boundary matrix

| Category / exported boundary | Implemented validation and conversion | Existing executed assertions | Truly missing bounded assertion / limit |
|---|---|---|---|
| B1 Identity, lifetime and replay positions (`SessionId`, `SessionLifetime`, cursors/pages, attach) | ID byte/control policy and literal equality; opaque lifetime components; replay checks exact lifetime and future position, publishes typed gaps. Numeric lifetime components are not independently documented as requiring nonzero values—do not invent such a validation rule. | `identity_enforces_byte_limit_and_control_policy`; `accepted_identity_discards_excess_owned_capacity_and_redacts`; `identities_and_future_positions_are_validated_even_when_empty`; `foreign_future_and_evicted_cursors_are_distinct_and_readers_are_independent`. | No new generic foreign/empty-cursor test needed. Complete marker coverage for compound transfer/output wrappers only if their public Debug implementation adds fields beyond these tested inner values (B10). |
| B2 Commands/environment/cwd and process values | `CommandSpec` rejects relative paths, NUL/oversized values and invalid environment keys; canonical filesystem root enforcement occurs in process infrastructure. Literal args and explicit environment policy are preserved. Portable `ProcessError` contains no raw errno or diagnostic string. | `commands_are_literal_bounded_and_redacted`; `environment_keys_validate_and_overrides_remain_last`; `synthetic_input_is_not_echoed_or_exposed_in_debug`. Existing real-child/root contracts are retained elsewhere; this row does not re-prove every filesystem edge. | Add only missing combined executable/cwd/removal/override marker assertions for normal/alternate Debug and emitted portable error formatting. Existing command tests use argument/value markers and primarily normal Debug; do not claim every path/key diagnostic surface is covered. |
| B3 Runtime/session/projection options, absent projection and dimensions | Runtime/session validators enforce relevant finite budgets and cross-option constraints before infrastructure spawn; terminal dimensions are validated values. Raw/projected choice is fixed at creation; projection-only access returns typed Unsupported when absent. | `domain_session_validation_rejects_incompatible_projection_admission` covers seven incompatible combinations plus observer bound; `rejects_invalid_dimensions_config_and_incompatible_checkpoints` covers zero/oversized grid and selected terminal config. | A small explicit raw-session table for each exposed projection-only operation should assert Unsupported, unchanged child identity/output and unchanged projection reservations. Search/reuse existing raw-mode tests before adding. Current selected tests do not establish every option at min/max/overflow or every absent-projection API. |
| B4 Replacement engine/capability and compatibility admission | Coordinator checks required checkpoint capability, nonempty compatibility and length≤4096 before terminal creation/process spawn; transient reservations unwind on rejection. Protector size-bound query occurs earlier, so “no side effects” must specifically count native create/process spawn and live reservations, not assert no provider method is called at all. | `rejected_projection_admission_settles_published_waiter_and_releases_identity_and_model_slot` proves factory **create failure** wakes a published waiter, starts no process, releases reservations and permits a fresh lifetime. It is not a capability-rejection test. | Highest-priority new table: checkpoints=false, compatibility empty and compatibility length 4097; count create/restore/process calls as zero, assert exact rejection and no retained session/native/request charge. For a valid factory with optional capabilities false, demonstrate the supported fallback behavior rather than rejecting all optional capabilities. No dynamic engine replacement API is exposed; do not invent one to satisfy a foreign-state row. |
| B5 Portable error layering (`RuntimeError`, `ProjectionError`, process/terminal/checkpoint errors) | Domain/application errors are closed enums; conversion preserves Process/Terminal/Storage boundary category. Runtime Display uses its redacted enum Debug. Rich OS/store errors are translated in infrastructure. | `completion_preserves_actual_exit_and_all_portable_failure_categories` enumerates portable process/terminal/checkpoint/runtime categories through external wire roundtrips and checks unknown codes. | No duplicate enum roundtrip table needed. Missing assertion is external-rich-error→portable-category mapping/redaction, especially event-sink variants with diagnostic strings/identities (B9). Keep callback panic containment and OS failure injection in R1, not a supposed format-only proof. |
| B6 Checkpoint/protected blob/descriptor/store boundary | Payload values may be constructed without validation; protector/store and coordinator validate at consumption. Authenticated metadata binds identity, generation, processed position, control generation and compatibility. Read lengths, descriptors and capacity are checked before ownership is accepted. | `ciphertext_corruption_truncation_wrong_key_and_metadata_replay_rejected`; `injected_provider_same_contract`; `opened_checkpoint_contract_violation_is_invalid_configuration_and_releases_read_permits`. | Existing cases already cover absent/truncated ciphertext, wrong owner/key and metadata replay. Extend only missing supplied-provider contract cases with exact typed outcome, no native restore call and permit/source preservation; do not run malformed native decoder inputs. Key erasure/lifetime audit is separate from proving payload Debug redaction. |
| B7 Native DTO→portable view/style conversion | Infrastructure owns codepoint/color/style conversion, rejecting invalid Unicode scalar values, unknown color tags and underline discriminants with EngineFailure; finite string budgets produce BudgetExceeded. Domain cells/styles remain engine-neutral. | Existing native dimension/config cases and broad terminal tests execute in the gate; the 22-name ledger here contains no direct injected `color`/`convert_style`/`decode` negative conversion test. | Small pure conversion table for unknown color/underline tags, surrogate/out-of-range codepoints and insufficient remaining text budget; assert exact error and no partial budget mutation where required. These can exercise Rust conversion functions without running native parser/crash work. Do not claim all native DTO/ABI states are thereby proved. |
| B8 Event record decoding: invalid, absent and foreign wire values | Schema/version/magic/tag, record cursor, size, truncation/trailing bytes, byte ranges and completion facts checked before portable output. Store cursor is never converted into PTY cursor. | `every_truncation_trailing_byte_and_invalid_envelope_is_rejected`; `contradictory_ranges_and_incomplete_or_unknown_completion_fields_are_rejected`; `independently_constructed_completion_wire_rejects_malformed_fields_and_truncation`; portable category roundtrip test above. | This boundary already has unusually strong finite negative coverage, including independently constructed wire and exhaustive single-byte perturbation. Do not request another broad invalid-envelope sweep. Missing wrapper marker formatting is B10, not missing decoding. |
| B9 Event publication receipts/external errors | Publisher preserves pending identity across cancellation/rejection/uncertain commit; receipt/schema/stream failures remain portable. `PublicationError::from(event_stream::Error)` groups capacity, unknown commit, identity conflict, unavailable and other errors while discarding external fields. | `exact_pending_identity_survives_rejection_uncertain_commit_and_invalid_receipts`; `cancelled_sink_wait_retries_same_id_before_and_after_commit`; B8 tests cover decoder categories. | Explicit external error-variant→SinkFailure table with synthetic marker-bearing fields and both Debug/Display of resulting PublicationError; no marker may survive. Existing pending-identity tests prove behavior, not every mapping branch or formatting path. |
| B10 Payload-bearing Debug wrappers and returned views | Manual Debug hides content in CommandSpec, SessionId, replay pages, terminal cells/effects/checkpoints/protected blobs. Derived outer wrappers inherit inner behavior but may add metadata. Some owning handles have no Debug implementation and should be marked not applicable, not “redaction tested.” | `opaque_checkpoints_hide_payload_and_adapter_metadata_in_debug`; `replay_and_cell_debug_redact_content_while_retaining_safe_position_and_style`; ID/command tests above. Both normal and alternate Debug are explicitly checked by the two domain marker tests. | Build a **finite** wrapper list from actual Debug implementations: TerminalEffects, complete TerminalView, OutputEvent/DecodedOutput, transfer event/boundary wrappers, and Publication (its external event cursor can contain caller metadata). Supply markers wherever each accepts text, assert formatting policy or explicitly document the external-metadata scope. Length-only Debug tests do not substitute for marker checks. No leak is claimed here without the missing assertion. |
| B11 Metrics/resources and failure-context diagnostics | Fixed numeric arrays/gauges/enums, no caller label maps; reader guard stores only count/bytes/Arc. Runtime diagnostics are opt-in and do not own payload. Fixture error records format portable public status/resources and retain typed original errors. | `raw_measurements_count_admission_actual_io_and_rejected_input_separately` plus reader/diagnostic tests elsewhere prove semantics and counts, not a complete synthetic-marker surface. | One real synthetic-marker operation sequence, then format/export every supported aggregate/latency/resource/error snapshot and assert marker absence. Include rejection as well as success. Keep intentionally returned raw output separate: the output API must return bytes; no-echo and no-diagnostic-disclosure are different contracts. Do not inspect a third-party raw transport payload and call its intentional contents a runtime log leak. |
| B12 Injected ports and closure/admission ownership | Public ports return translated portable values; tickets/leases and immutable sources retain ownership across abandoned waits, failure and closure. Services/capabilities are application contracts, not serialized DTOs. | Factory rollback, opened-checkpoint violation and publication tests above cover selected injected boundaries; existing request/full-parser and lost-completion suites add ownership proof. | For R2, limit additions to B4 capability/compatibility admission and B6 invalid provider-result checks. Full callback/acquisition/concurrency fault coverage belongs to R1/R3. Do not infer every port implementation is correct because it implements an interface. |

## Small actionable next set

1. B4 capability/compatibility admission table with explicit side-effect counters and reservation rollback. This is the clearest missing test: the existing factory-create-error test does not reach these rejection causes.
2. B9 rich external event error mapping plus marker exclusion; keep existing receipt/retry tests intact.
3. B10/B11 marker matrix for supported public Debug wrappers and emitted diagnostic snapshots, explicitly excluding non-Debug handles and intentional byte-returning APIs. Treat external transport metadata policy as a distinct scope decision if needed.
4. B7 pure infrastructure DTO negative conversion table, requiring no native process/parser execution. First reconcile these exact branches against coverage to avoid duplicating hidden tests.
5. Only then fill specific B3/B6 absent-projection/provider-result holes after matching existing test sources. No broad repeated wire fuzzing or new runtime API is justified by this inventory.

These are proof gaps, not established production bugs. Preserve a real failing behavior before any resulting production fix; a mistaken fixture or new-feature test does not establish an old runtime defect. The 22 matched tests remain valid scoped evidence, not proof of all re-exported methods/types or all future injected providers.
