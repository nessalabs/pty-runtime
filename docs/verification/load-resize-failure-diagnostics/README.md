# Projected resize failure diagnostics

The retained real trigger is candidate-2 `dominant-5.jsonl`: the fifth ordinary dominant load repeat returned `Projection(Capacity)` without indicating which resize boundary or producer failed. See `docs/reviews/load-dominant-capacity-diagnosis.md` and its source-bound evidence. That failure remains unresolved; this change does not fix or reproduce the underlying capacity condition.

The fixture now records `operation_failure` on admission, timeout or outer completion errors before propagation and population cleanup. Fields include phase, producer, probe, requested grid, monotonic phase elapsed time, original portable error, immediate public projection/process status and concurrent shared resource snapshot. Diagnostic status failures retain their `Err` representation. Snapshot fields are redacted Debug strings within the JSON record. Diagnostic write errors are ignored so they cannot replace the original resize error. Inner OS/model outcomes still reach the caller's existing assertion.

The wrapper makes one admission call and awaits the admitted operation once with the same thirty-second deadline. No retry, quota adjustment or runtime/native change was made. Request/staging local quota attribution remains unavailable through the public resource snapshot; later snapshots can race with quota release. Do not interpret this diagnostic as proof of local slot exhaustion.

The eight focused example tests pass, including three new boundary contracts: identical capacity errors at admission and completion produce distinct stages but retain the same concrete RuntimeError; an indefinitely pending operation produces the original Tokio Elapsed type; inner OS/model failure values remain unchanged for the existing caller policy. These tests validate the instrumentation boundary using supplied operation outcomes, not an OS-level reproduction or fix of the capacity incident. Clippy passes with warnings denied. Logs are retained transcripts of executed commands. Source fingerprints bind the implementation. The complete repository gate and any diagnostic load run remain the coordinating task's work; no Linux workload or release binary was changed.

Independent source review completed with no P1/P2 findings: `docs/reviews/load-resize-diagnostic-independent.md`. The independent reviewer inspected the tests but did not re-execute them; executed verification is the author run recorded here.
