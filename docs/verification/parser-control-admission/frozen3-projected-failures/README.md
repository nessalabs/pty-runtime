# Five full-configuration projected-capacity failures

These are the completed five projected-capacity trials from frozen checkpoint
5373d8e. Every trial failed in warmup with Projection(Capacity), before its
60-second measurement phase. Each retains full source/binary/host/configuration
identity and process exit evidence. The encompassing driver continues other
cases; this directory contains only the five already completed and closed
failure records, not a completed whole-matrix summary.

The later failure-stage diagnostic and deterministic red/green regression are
recorded in the parent directory and neighboring load-resize-failure-diagnostics.
