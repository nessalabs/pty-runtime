# Candidate 4 Linux validation

Full gate and release example build passed with unchanged source. The 291-file source inventory exactly matches the recorded macOS candidate4 build. Initial clone setup lacked a checked-out HEAD; no source validation ran in that setup attempt. The successful records here follow explicit checkout of 487ef087. Subsequent reader instrumentation requires its own validation.
