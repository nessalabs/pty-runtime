[package]
name = "pty-experiment-harness"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libc = "=0.2.189"
serde_json = "=1.0.151"

[profile.release]
debug = 1

# This fixture is independently runnable and keeps its own lockfile.
[workspace]
